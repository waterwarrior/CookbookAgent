AWSTemplateFormatVersion: '2010-09-09'
Description: 'Serverless AI Recipe Book Infrastructure'

Parameters:
  TelegramBotToken:
    Type: String
    Description: 'Token received from BotFather'
    NoEcho: true
  BedrockAgentId:
    Type: String
    Description: 'ID of your Bedrock Agent'
    Default: 'PLACEHOLDER'
  BedrockAgentAliasId:
    Type: String
    Description: 'Alias ID of your Bedrock Agent'
    Default: 'PLACEHOLDER'
  AllowedTelegramUserIds:
    Type: CommaDelimitedList
    Description: 'Comma-separated list of allowed Telegram user IDs'
    Default: ''

Resources:
  # 1. DynamoDB Table for Recipes
  RecipeTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: RecipesTable
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: RecipeId
          AttributeType: S
      KeySchema:
        - AttributeName: RecipeId
          KeyType: HASH

  # 2. IAM Role for Action Group Lambda
  BedrockActionGroupRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                  - dynamodb:GetItem
                  - dynamodb:UpdateItem
                  - dynamodb:Scan
                Resource: !GetAtt RecipeTable.Arn

  # 3. Action Group Lambda (Bedrock Executor)
  ActionGroupLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: RecipeActionGroupHandler
      Handler: index.lambda_handler
      Role: !GetAtt BedrockActionGroupRole.Arn
      Runtime: python3.12
      Timeout: 15
      Environment:
        Variables:
          TABLE_NAME: !Ref RecipeTable
      Code:
        # language=python
        ZipFile: |
            import json
            import os
            import boto3
            from boto3.dynamodb.conditions import Attr

            dynamodb = boto3.resource('dynamodb')

            def lambda_handler(event, context):
                print("Bedrock Agent called this:", json.dumps(event))
                table_name = os.environ['TABLE_NAME']
                table = dynamodb.Table(table_name)

                # Extract session_id passed by the webhook
                session_id = event.get('sessionId', 'unknown_user')

                response_content = ""
                function_name = event.get('function', '')
                params = event.get('parameters', [])

                try:
                    if function_name == 'save_recipe':
                        recipe_name = next((p['value'] for p in params if p['name'] == 'recipe_name'), None)
                        recipe_content = next((p['value'] for p in params if p['name'] == 'recipe_content'), None)
                        # Optional parameter: tags
                        tags = next((p['value'] for p in params if p['name'] == 'tags'), 'Без тегов')

                        if recipe_name and recipe_content:
                            table.put_item(Item={
                                'RecipeId': recipe_name.lower(),
                                'OriginalName': recipe_name,
                                'Content': recipe_content,
                                'UserId': session_id,  # Save author
                                'Tags': tags           # Save tags
                            })
                            response_content = f'Рецепт "{recipe_name}" (Теги: {tags}) успешно сохранен!'
                        else:
                            response_content = "Ошибка: не передано имя или текст рецепта."

                    elif function_name == 'search_recipes':
                        keyword = next((p['value'] for p in params if p['name'] == 'keyword'), '').lower()
                        # Additional filter parameters
                        only_my = next((p['value'] for p in params if p['name'] == 'only_my'), 'false').lower() == 'true'
                        names_only = next((p['value'] for p in params if p['name'] == 'names_only'), 'false').lower() == 'true'

                        fe = None
                        if keyword:
                            # Match by RecipeId or Tags
                            fe = Attr('RecipeId').contains(keyword) | Attr('Tags').contains(keyword)
                        
                        # Filter by author if requested
                        if only_my:
                            user_fe = Attr('UserId').eq(session_id)
                            fe = fe & user_fe if fe else user_fe

                        if fe:
                            response = table.scan(FilterExpression=fe)
                        else:
                            response = table.scan()

                        items = response.get('Items', [])

                        if not items:
                            response_content = "Рецепты по данному запросу не найдены."
                        else:
                            if names_only:
                                # "Table of contents" mode: return formatted names only
                                matches = [item.get('OriginalName', item['RecipeId']) for item in items]
                                response_content = "Список рецептов в базе: \n- " + "\n- ".join(matches)
                            else:
                                # Full search mode (limit to 5 items to avoid blowing up context window)
                                matches = [{'name': item.get('OriginalName', item['RecipeId']), 'tags': item.get('Tags', ''), 'recipe': item.get('Content', '...')} for item in items][:5]
                                response_content = json.dumps(matches, ensure_ascii=False)

                    elif function_name == 'update_recipe':
                        recipe_name = next((p['value'] for p in params if p['name'] == 'recipe_name'), None)
                        new_content = next((p['value'] for p in params if p['name'] == 'new_content'), None)
                        new_tags = next((p['value'] for p in params if p['name'] == 'tags'), None)

                        if recipe_name and new_content:
                            recipe_id = recipe_name.lower()
                            response = table.get_item(Key={'RecipeId': recipe_id})

                            if 'Item' in response:
                                original_name = response['Item'].get('OriginalName', recipe_name)
                                
                                update_expr = "set Content = :c"
                                expr_attrs = {':c': new_content}
                                
                                # Update tags if provided
                                if new_tags is not None and new_tags.strip():
                                    update_expr += ", Tags = :t"
                                    expr_attrs[':t'] = new_tags
                                    
                                table.update_item(
                                    Key={'RecipeId': recipe_id},
                                    UpdateExpression=update_expr,
                                    ExpressionAttributeValues=expr_attrs
                                )
                                response_content = f'Рецепт "{original_name}" успешно обновлен!'
                            else:
                                response_content = f'Ошибка: Рецепт "{recipe_name}" не найден. Сначала найди его точное название.'
                        else:
                            response_content = "Ошибка: не передано имя или обновленный текст."
                    else:
                        response_content = "Неизвестная операция."

                except Exception as e:
                    print("DynamoDB Error:", e)
                    response_content = f"Ошибка БД: {str(e)}"

                return {
                    "messageVersion": "1.0",
                    "response": {
                        "actionGroup": event.get('actionGroup', ''),
                        "function": function_name,
                        "functionResponse": {
                            "responseBody": {
                                "TEXT": {
                                    "body": response_content
                                }
                            }
                        }
                    }
                }

  # Permission for Bedrock to invoke Action Group Lambda
  BedrockInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt ActionGroupLambda.Arn
      Action: lambda:InvokeFunction
      Principal: bedrock.amazonaws.com

  # 4. IAM Role for Telegram Webhook Lambda
  TelegramWebhookRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: BedrockInvokeAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - bedrock:InvokeAgent
                  - bedrock:InvokeModel
                  - aws-marketplace:ViewSubscriptions
                  - aws-marketplace:Subscribe
                Resource: "*"

  # 5. Telegram Webhook Lambda (Integration layer)
  TelegramWebhookLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: TelegramWebhookReceiver
      Handler: index.lambda_handler
      Role: !GetAtt TelegramWebhookRole.Arn
      Runtime: python3.12
      Timeout: 59
      Environment:
        Variables:
          TELEGRAM_TOKEN: !Ref TelegramBotToken
          AGENT_ID: !Ref BedrockAgentId
          AGENT_ALIAS_ID: !Ref BedrockAgentAliasId
          ALLOWED_CHATS: !Join [",", !Ref AllowedTelegramUserIds]
      Code:
        # language=python
        ZipFile: |
            import json
            import os
            import boto3
            import urllib.request

            bedrock_agent = boto3.client('bedrock-agent-runtime')
            bedrock_runtime = boto3.client('bedrock-runtime')

            def send_message(token, chat_id, text):
                if not text or not text.strip():
                    print("⚠️ Attempt to send an empty message to Telegram. Skipping.")
                    return

                print(f"📤 Sending message to chat {chat_id}: {text[:50]}...")
                url = f"https://api.telegram.org/bot{token}/sendMessage"
                payload = json.dumps({'chat_id': chat_id, 'text': text}).encode('utf-8')
                req = urllib.request.Request(url, data=payload, headers={'Content-Type': 'application/json'})
                try:
                    urllib.request.urlopen(req)
                except Exception as e:
                    print(f"❌ Error sending to Telegram: {str(e)}")

            def lambda_handler(event, context):
                tg_token = os.environ['TELEGRAM_TOKEN']
                agent_id = os.environ['AGENT_ID']
                agent_alias_id = os.environ['AGENT_ALIAS_ID']
                allowed_chats_raw = os.environ['ALLOWED_CHATS']
                allowed_chats = [int(i.strip()) for i in allowed_chats_raw.split(',') if i.strip()]

                if agent_id == 'PLACEHOLDER':
                    return {'statusCode': 200, 'body': 'Agent ID not configured'}

                try:
                    body = json.loads(event.get('body', '{}'))
                    
                    if 'message' not in body:
                        return {'statusCode': 200, 'body': 'OK'}

                    chat_id = body['message']['chat']['id']

                    if chat_id not in allowed_chats:
                        print(f"🚫 Access denied for chat: {chat_id}")
                        return {'statusCode': 200, 'body': 'OK'}

                    user_text = body['message'].get('text', '')
                    photo = body['message'].get('photo', None)
                    caption = body['message'].get('caption', '')

                    response = None

                    # 1. HANDLE /new COMMAND
                    if user_text.strip() == '/new':
                        bedrock_agent.invoke_agent(
                            agentId=agent_id, 
                            agentAliasId=agent_alias_id,
                            sessionId=str(chat_id), 
                            inputText="End session", 
                            endSession=True
                        )
                        send_message(tg_token, chat_id, '👨‍🍳 Память очищена! Готов к новому рецепту.')
                        return {'statusCode': 200, 'body': 'OK'}

                    # 2. HANDLE PHOTOS
                    elif photo:
                        file_id = photo[-1]['file_id']
                        get_file_url = f"https://api.telegram.org/bot{tg_token}/getFile?file_id={file_id}"
                        file_data = json.load(urllib.request.urlopen(get_file_url))
                        file_path = file_data['result']['file_path']
                        image_bytes = urllib.request.urlopen(f"https://api.telegram.org/file/bot{tg_token}/{file_path}").read()

                        vision_model_id = 'us.anthropic.claude-sonnet-4-6'

                        converse_response = bedrock_runtime.converse(
                            modelId=vision_model_id,
                            messages=[{"role": "user", "content": [
                                {"image": {"format": "jpeg", "source": {"bytes": image_bytes}}},
                                {"text": "Внимательно прочитай весь текст рецепта с этой картинки. Если это не рецепт, просто опиши."}
                            ]}]
                        )
                        extracted_text = converse_response['output']['message']['content'][0]['text']

                        agent_prompt = f"Я сфотографировал рецепт. Текст:\n\n{extracted_text}\n\nТвоя задача: ВЫВЕДИ ЭТОТ ТЕКСТ В MARKDOWN И СПРОСИ МОЕГО РАЗРЕШЕНИЯ НА СОХРАНЕНИЕ. КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО вызывать функции сохранения до моего подтверждения. Мой комментарий: {caption}."
                        
                        response = bedrock_agent.invoke_agent(
                            agentId=agent_id, agentAliasId=agent_alias_id,
                            sessionId=str(chat_id), inputText=agent_prompt
                        )

                    # 3. HANDLE NORMAL TEXT
                    elif user_text:
                        response = bedrock_agent.invoke_agent(
                            agentId=agent_id, agentAliasId=agent_alias_id,
                            sessionId=str(chat_id), inputText=user_text
                        )

                    # PARSE AGENT RESPONSE
                    if response:
                        completion = ""
                        for stream_event in response.get('completion', []):
                            if 'chunk' in stream_event:
                                completion += stream_event['chunk']['bytes'].decode('utf-8')

                        send_message(tg_token, chat_id, completion)

                except Exception as e:
                    error_info = f"🚨 Lambda Error: {str(e)}"
                    print(error_info)
                    send_message(tg_token, chat_id, error_info)

                return {'statusCode': 200, 'body': 'OK'}

  # 6. HTTP API Gateway
  HttpApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: TelegramWebhookApi
      ProtocolType: HTTP

  LambdaIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt TelegramWebhookLambda.Arn
      PayloadFormatVersion: '2.0'

  HttpApiRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: 'POST /webhook'
      Target: !Join ['/', ['integrations', !Ref LambdaIntegration]]

  HttpApiStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      ApiId: !Ref HttpApi
      StageName: prod
      AutoDeploy: true

  ApiGatewayInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt TelegramWebhookLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Join ['', ['arn:aws:execute-api:', !Ref 'AWS::Region', ':', !Ref 'AWS::AccountId', ':', !Ref HttpApi, '/*/*/webhook']]

Outputs:
  WebhookUrl:
    Description: 'Set this URL in Telegram via setWebhook'
    Value: !Join ['', [!GetAtt HttpApi.ApiEndpoint, '/prod/webhook']]